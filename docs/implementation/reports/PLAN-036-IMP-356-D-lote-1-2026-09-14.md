# PLAN-036 - IMP-356-D lote 1: catálogo, dispatcher e apresentadores (2026-09-14)

Branch: `feat/imp-356-d-catalogo`. Escopo: catálogo fechado + cliente HTTP +
dispatcher + apresentadores determinísticos. Fora do escopo (lote 2+):
cliente LLM BYOK, executor com deadline, sessão/vínculo de refs, triagem e
certificação com modelo real.

## Arquivos

- `src/emprestimo/agent/catalogo.py` — `consulta_operadora_v1`: 6 leituras,
  3 permissões (`devedor.ler`, `motor.saldo.ler`,
  `relatorios.operacionais.ler`), dataclasses frozen.
- `src/emprestimo/agent/api_client.py` — `httpx.AsyncClient` contra a API do
  produto; 401/403 → `ApiAutorizacaoError`, 404 → `ApiAusenciaError`.
- `src/emprestimo/agent/dispatcher.py` — valida nome/argumentos, monta URLs
  fixas, carimba `data_referencia`/`hoje` do servidor, resolve `devedor_ref`
  opaca via resolvedor injetado (o vínculo de sessão é do 356-F; sem
  resolvedor padrão embarcado).
- `src/emprestimo/agent/apresentadores.py` — texto fixo por ferramenta;
  máscara nome/documento, preserva decimais, descarta projeção, 404 ≠ zero.
- Correção adjacente: `recusar_principal_ausente` agora levanta
  `AutenticacaoRecusadaError` (antes só auditava e caía em 500 via
  `AssertionError`).

## Provas (todas verdes)

| Prova | Onde | Resultado |
|---|---|---|
| T1 catálogo congelado (6 tools, permissões, frozen) | `test_catalogo_operadora.py` | 1 ok |
| T2 nome/URL/permissão fixos (chamadas capturadas) | idem | 5 ok |
| T3 bateria anti-fraude (fantasma, extra, ID cru, injeção, URL, campo inventado, datas) | idem | 12 params + 8 ok |
| Apresentadores (máscara, decimais, rótulos, 404≠zero, sem IDs) | idem | 10 ok |
| T4 ponta a ponta real (app + serviços + PostgreSQL via ASGI) | `test_catalogo_api_real.py` | 5 ok |
| Regressão auth/autorização/aplicação | suites existentes | 147 ok |
| Regressão agente (unit + integration) | suites existentes | 109 ok, 1 skip pré-existente |
| ruff / black / mypy (5 arquivos) | local | limpos |

## Achados do T4 (corrigidos no lote)

1. Listagem real retorna `items[]` com documento/contatos crus — o
   mascaramento vive no apresentador (ponto único entre API e modelo), que lê
   só `id/nome/documento/estado`. Nada cru vaza por construção.
2. `GET .../saldo` exige `data_referencia`: dispatcher carimba com o hoje do
   servidor. Sem Bearer a API devolvia 500; agora 401 `autenticacao_recusada`.

## Pendências

- Lote 2: cliente LLM BYOK + triagem/certificação com modelo real.
- 356-F: resolvedor de produção (registro opaco por sessão) e executor.

---

# Histórico de Versões

| Versão | Data | Descrição |
|---|---|---|
| 1.0.0 | 2026-09-14 | Evidência do lote 1: T1–T4 verdes, 2 achados corrigidos, pendências declaradas. |
